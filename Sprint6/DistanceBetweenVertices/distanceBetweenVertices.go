package distanceBetweenVertices

import (
	"io"
	"bufio"
	"fmt"
)

func DistanceBetweenVertices(r io.Reader, w io.Writer){
	reader := bufio.NewReader(r)
	//writer := bufio.NewWriter(w)
	fmt.Println(reader)
}